#include <stdio.h>

const double PI = 3.142;

double C_area(double rad)
{
    return PI * rad * rad;
}


int main()
{
    double rad = 25;
    double height = 4;

    double vol = C_vol()
    double a = C_area(rad);

    // printf("The area: %2f", a);
}