#include "main.h"
#include <stdio.h>

int main()
{
    // char C[5] = {'J', 'O', 'H', 'N', '\0'};

    // int len = StrLen(C);

    // printf("%d",len);

    char c[20] = {'h', 'e', 'l', 'l', 'o'};

    Print(c);

    return (0);
}