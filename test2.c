#include <stdio.h>
// #include <stdlib.h>
#include <string.h>


int main(void)
{
    char *s;
    int *ptr;
    *ptr = 98;

    s = (char *) malloc(100);

    // strcpy(s, "Best School");

    // // printf("%s", s);
    // printf("%d", *ptr);


    return (0);
}